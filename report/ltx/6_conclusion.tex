\section{Conclusions}

% STRUCTURE (SK-6; Knoll 06-04: NOT a summary -- closes the circle, half a page to ONE page max):
% 2-3 sentence preamble, then one block per objective (bold lead-ins), each a "what was learned" statement:
%   Objective 1 -- pump characterisation (psi ~ 1.10 vs design 1.065; eta 19% -> 26.4% at 20k; churning MEASURED 2.5x; dual-site cavitation visualised)
%   Objective 2 -- turbine design + model validation (MoC/Goldman/Colclough/Weiss -> ~47%; brake test NOT performed -- say so; starting criterion load-bearing)
%   Objective 3 -- integration (~5,400 rpm coupled; gap = supply droop + unstart, both named)
%   Objective 4 -- toolchain assessment (Lock adequate for design-stage screening; 1D sufficient at this scale; CFD = future work)
% Overarching: one sentence, NO primacy claims.

\subsection{Future Work}

\todo{future work: find the cavitation curve that lock describes, which can only be seen by slowly closing the outlet valve instead of what I did which is slowly closing inlet valve. basically, just more test as none of my data was able to capture what he found... unless I need to analyze the H-Q curves. Yes I will do that.}